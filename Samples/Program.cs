using Samples.Files;
using System;
using System.Collections.Generic;
using System.Text;

namespace Samples
{
    public static class Program
    {
        public static void Main()
        {
            //var stringFormats = new StringFormatting.StringFormats();

            //Console.WriteLine(stringFormats.FormatUsingInterpolation());
            //Console.WriteLine(stringFormats.FormatUsingComposite());

            //var textFile = AppContext.BaseDirectory + "\\TextFile.txt";
            //var textFileBasics = new TextFileBasics();
            //textFileBasics.WriteToTextFile(textFile, "This is a single line of text.");
            //Console.WriteLine(textFileBasics.ReadAllLinesFromTextFile(textFile));
            //textFileBasics.WriteLinesToFile(textFile, new string[] { "A line of text.", "The second line of text.", "And the third." });

            Console.ReadKey();
        }
    }
}
